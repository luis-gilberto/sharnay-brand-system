# Sharnay Visual Language System · temporary Vercel surface

Publishes the canonical file:

`reading-the-practice/Sharnay_Photography_Visual_Language_System_ES-EN.html`

at the project root URL via a build-time copy (`copy-vls.mjs`). No divergent VLS source is maintained here.

Production alias (temporary until custom DNS):

`https://sharnay-visual-system.vercel.app`

El Retrato remains on the separate `reading-the-practice` Vercel project.
